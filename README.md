Creacion de una API con FastApi y data simulada de una BD.

Eliminacion de carpeta __pycache__ con .gitignore colocar en el archivo: __pycache__/
  git rm -r --cached __pycache__/
  git commit -m "Remove __pycache__ from version control"
